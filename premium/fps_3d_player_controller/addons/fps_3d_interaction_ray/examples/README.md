# Examples

Bundled with FPS 3D Player Controller Pro. The packed `ga_fps_player.tscn` already has this ray on the camera.

Enable **FPS 3D Interaction Ray** in Project Settings > Plugins if you want the plugin listed; the script itself is used by the prefab via `res://addons/fps_3d_interaction_ray/core/scripts/ga_interaction_ray.gd`.
