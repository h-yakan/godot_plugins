# Examples

See the kit greybox apartment: `res://examples/greybox_apartment/`.

Hold **Q+E** on the horror player prefab. Requires an audio bus named `World` with a LowPass effect (the demo ships `default_bus_layout.tres`).
